// 4 - Faça um algoritmo que leia três notas obtidas por um aluno, e imprima na tela a média das notas

const a = (Number(prompt('nota unidade 1:')))
const b = (Number(prompt('nota unidade 2:')))
const c = (Number(prompt('nota unidade 2:')))
var media

function calculoMedia (n1, n2, n3) {
    const notas = (n1 + n2 + n3) / 3; {
        console.log(`sua media é: ${notas}`);
    };
    media = notas;
    return 
};
calculoMedia(a, b, c);







